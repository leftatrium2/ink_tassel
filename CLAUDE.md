## 项目概述

+ 软件名：墨流苏
+ 编译后目标文件名：ink_tassel
+ 文本编辑器项目，字符界面，按ESC呼出菜单
+ 界面设计，参照dos环境下CCED文本编辑器

# 要求

+ 使用  c 语言编写，符合 c89 规范
+ 使用 ncurses 库编写
+ 使用CMake方式编译
+ 按照功能规划源文件，保证每个源文件行数小于1000行

# 编码规范

+ 不使用制表符，而是使用空格，每个缩进级别使用4个空格

+ 使用蛇形风格，也就是内核风格（unix_like），比如：test_result

+ 宏、枚举值采用全大写，下划线连接的格式。

+ 全局变量应增加 'g_' 前缀，函数内静态变量命名不需要加特殊前缀

+ 锁紧使用BSD风格（Allman风格）

# 功能列表

+ 菜单：
  + 文件：
    + 保存文件
    + 存盘退出
    + 放弃存盘
    + 读文件
    + 设置密码
    + 【菜单分割线】
    + 设置
  + 编辑
    + 撤销 
    + 重做
    + 【菜单分割线】
    + 查找
  + 云存储
    + 同步
  + 帮助
    + 关于
  
+ 菜单功能说明
	+ 菜单->文件->设置
	
	  【点击后，提示：暂无此功能】
	
	+ 菜单->云存储->同步
	
	  【点击后，提示：暂无此功能】